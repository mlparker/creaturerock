<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Lora:wght@400;700&family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="{{ site.baseurl }}/styles.css" />
</head>
<body>
  <div class="site-wrapper">
    <header class="site-header">
      <nav>
        <a href="{{ site.baseurl }}/">Home</a>
        <a href="{{ site.baseurl }}/bio/">Bio</a>
        <a href="{{ site.baseurl }}/projects/">Projects</a>
        <a href="{{ site.baseurl }}/media/">Media</a>
        <a href="{{ site.baseurl }}/news/">News</a>
        <a href="{{ site.baseurl }}/press-kit/">Press Kit</a>
      </nav>
    </header>

    <main class="site-main">
      {{ content | safe }}
    </main>

    {% if image %}
      <div class="post-featured-image">
        <img src="{{ site.baseurl }}{{ image }}" alt="{{ title }}" />
      </div>
    {% endif %}

    {% if bandcamp_embed %}
      <div class="bandcamp-widget">
        {{ bandcamp_embed | safe }}
      </div>
    {% endif %}
    
    {% if contact_form %}
      <div class="contact-form">
        {{ contact_form | safe }}
      </div>
    {% endif %}

    <footer class="site-footer">
      <p>&copy; {{ title }} — Creature Rock transmissions</p>
    </footer>
  </div>
<script src="https://cdn.jsdelivr.net/npm/tsparticles@3/tsparticles.bundle.min.js" defer></script>
<script src="{{ site.baseurl }}/scripts/particles-init.js" defer></script>
<div id="tsparticles"></div>
</body>
</html>
